header
    i.fas.fa-bars.fa-lg
    div.menu
    a(href="https://www.youtube.com/c/dlwlrma")  아이유 ✨
    a(href="https://www.youtube.com/channel/UCtF9Njo6rLI8U2ZrJ_cFzHQ")  러블리즈 ✨
    a(href="/").header__logo
        i.fab.fa-youtube.fa-lg IUTUBE
        
    nav
        ul
            li
                a(href="/search")
                    i.fas.fa-search
            if loggedIn
                li
                    a(href="/videos/upload") Upload Video
                li
                    a(href="/users/edit") Edit Profile
                li
                    a(href="/users/logout")  Log Out
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            if loggedInUser.avatarUrl   
                                if loggedInUser.avatarUrl.includes("github")
                                    img.header__avatar(src=loggedInUser.avatarUrl,crossorigin)
                                else
                                    img.header__avatar(src=loggedInUser.avatarUrl,crossorigin)
            else             
                li
                    a(href="/login").fas.fa-user.fa-lg  Login                        
                li
                    a(href="/join").fas.fa-sign-in-alt.header__btn.fa-lg  Join